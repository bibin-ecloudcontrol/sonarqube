app:
  name: SonarQube
  code: SonarQube 
  notify: bkoshy@ecloudcontrol.com
    
build:
  version: 8.2
  env: DEV
  build_file: none
  output_files: output/*.zip
  image_template: sonarqube_azl-8.2
      
deploy:
  context: bibin-ecloudcontrol/DEV
  replicas: 1
  port:
  - 9000
    
monitor:
  notify: bkoshy@ecloudcontrol.com      
    
properties:
  sonar.forceAuthentication: true
  SONAR_ADMIN_USERNAME: ecloudsys
  SONAR_ADMIN_PASSWORD:
    vault: SONAR_ADMIN_PASSWORD
  SONAR_USER_USERNAME: eclouduser
  SONAR_USER_PASSWORD: 
    vault: SONAR_USER_PASSWORD
  SONAR_JDBC_URL: jdbc:postgresql://postgres-sonarqube-12-2:5432/sonar
  SONAR_JDBC_USERNAME: sonaradmin
  SONAR_JDBC_PASSWORD: 
    vault: SONAR_JDBC_PASSWORD
  ENABLE_OIDC: TRUE  
  CREATE_DEFAULT_USERS: TRUE
  GF_AUTH_GENERIC_OAUTH_ENABLED: true
  GF_AUTH_GENERIC_OAUTH_NAME: jcarver
  GF_AUTH_GENERIC_OAUTH_CLIENT_ID: sonarqube
  GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET: secretex
  GF_SERVER_ROOT_URL: https://bibin-ecloudcontrol-dev-sonarqube-8-2.32d430c5.lowtouch.cloud
  revision : 39
